doctype html
html
    head
        title= pageTitle
        block css
            link(rel="stylesheet" href="/bootstrap-4.1.3-dist/css/bootstrap.min.css")
    body(class="container_fluid")
        div(class="row justify-content-center")
            div(class="row")
                header(class="col-12")
                    block header
                        h2 Hello 
                            if user
                                span #{user.prenom} #{user.nom}
                nav(class="col-md-3")
                    block nav
                        ul
                            li 
                                a(href="/") Acceuil
                            li 
                                a(href="/contact") Contact
                            if user 
                                li 
                                    a(href="/logout") Déconnection
                                li
                                    a(href="/novarticle") Nouvel article

                            else
                                li 
                                    a(href="/inscription") Inscription
                                li
                                    a(href="/login") Connection 
                div(class="col-md-9")        
                    block content
                footer(class="col-12")
                    block footer
                        p &copy; moi
            block js
                script(src="/jquery.min.js") 
                script(src="/bootstrap-4.1.3-dist/js/bootstrap.min.js")       